# JSMonitor — Панель мониторинга игровых серверов

> **v2.4.0** · Go + Next.js 14 + MySQL · Docker Compose · Glassmorphism UI

Высокопроизводительная реактивная панель мониторинга игровых серверов с real-time обновлениями, системой аккаунтов, новостями, Discord/Telegram интеграцией и полноценной панелью администратора.

---

## Возможности

### Мониторинг серверов
- **Real-time WebSocket** — мгновенное обновление статуса без перезагрузки, экспоненциальный реконнект
- **Поддерживаемые игры**: Source Engine, Garry's Mod, Valheim, Squad, DayZ, V Rising, Icarus, Minecraft Java/Bedrock, FiveM / GTA V, SA-MP / open.mp, Terraria
- **История онлайна** — графики за 24ч / 7д / 30д (Recharts)
- **Список игроков** — A2S, SA-MP и Minecraft query в реальном времени + Minecraft MOTD
- **Пинг** — история пинга рядом с историей онлайна
- **Автоопределение** — название сервера и флаг страны (GeoIP) при добавлении
- **Display IP** — показывать на карточке домен вместо реального IP
- **Кнопка «Играть»** — deeplink к клиенту (`steam://`, `fivem://`, `samp://`, `minecraft://`)
- **Копирование IP** и шеринг ссылки на сервер
- **Занятость** — цветовой индикатор заполненности (зелёный / жёлтый / красный)
- **Аптайм** — процент доступности сервера за последние 24 ч

### Интерфейс
- **Светлая / тёмная / системная тема** — переключение в один клик, сохраняется в localStorage
- **Поиск, фильтры, сортировка** — по игре, статусу, игрокам, пингу, названию
- **Избранное** — закрепление серверов вверху списка
- **Мобильное меню** — адаптивный дизайн для любых экранов
- **Мультиязычность** — интерфейс на русском и английском (переключение без перезагрузки)
- **Glassmorphism UI** — тёмная тема с neon-акцентами и эффектами свечения
- **Командная палитра** — быстрый поиск по серверам и страницам
- **Toasts** — уведомления об успехе / ошибках

### Сравнение серверов
- Страница `/compare?ids=1,2,3` — графики онлайна нескольких серверов на одной оси
- Выбор до N серверов прямо на главной странице

### Встраиваемый виджет
- Эндпоинт `/embed/:serverID` — лёгкий виджет для вставки на сторонний сайт через `<iframe>`
- SVG-график онлайна, автообновление каждые 30 сек, адаптивный

### Аккаунты и профили
- Регистрация по логину/паролю, вход через **Steam OpenID**
- **Профиль пользователя** (`/profile`): аватар (WebP 256×256), имя, email, пароль, Steam ID, API-токен, удаление аккаунта
- **Публичная страница** (`/u/:username`) — серверы пользователя, роль, дата регистрации
- **Удаление аккаунта** — 7-дневный период ожидания с возможностью отмены
- JWT авторизация (7 дней) с опцией «Запомнить меня»
- **2FA / TOTP** — двухфакторная аутентификация через Google Authenticator / Authy (QR-код)
- **Управление сессиями** — список активных сессий, отзыв отдельных, «выйти везде»
- **Сброс пароля** — через email-ссылку (SMTP)
- Владение серверами — редактировать/удалять можно только свои

### Лидерборды и профили игроков
- **Глобальный лидерборд** — рейтинг игроков по суммарному времени на серверах
- **Профиль игрока** (`/player/:name`) — суммарное время, список серверов, последний онлайн
- **Сессии игроков** — трекинг начала/конца каждой сессии

### Новости / объявления
- Markdown-редактор с предпросмотром, поддержка изображений
- Аватар автора, время чтения, метка «изменено»
- Теги, закреплённые посты, черновики, отложенная публикация
- Счётчик просмотров
- Публикация в Discord и Telegram — авто-обновление при редактировании

### Уведомления
- **Telegram-алерты** — уведомление при переходе сервера офлайн/онлайн (настраивается на каждый сервер)
- **Email-алерты** — те же события через SMTP (SMTP_* ENV переменные)
- **Discord-алерты** — уведомления в отдельный канал Discord

### Discord Gateway Bot
- Команда **`/addserver <id>`** — постоянный эмбед с графиком онлайна прямо в Discord-канале
- Кнопки переключения периода графика: **24ч / 7д / 30д** + кнопка открытия Админки
- **Авторефреш** — эмбед автоматически обновляется с настраиваемым интервалом (мин. 10 сек)
- **Персистентность** — эмбеды сохраняются в БД и восстанавливаются после перезапуска бота
- **Команда `/stats`** — глобальная статистика: серверы, игроки, пик за 24 ч
- **Команда `/top`** — топ-10 игроков сегодня по времени онлайна
- **Ротирующий статус бота** — каждые 60 сек: кол-во онлайн-серверов / топ-игрок / аптайм
- **Кастомный цвет эмбеда** — задаётся через hex в настройках каждого сервера
- **Логотип сайта в футере** — берётся из настроек (раздел «Логотип»)
- **Discord Timestamp** — время обновления отображается в локальном времени пользователя
- Поддержка **HTTP-прокси** для Discord Gateway (HTTPS_PROXY)

### Telegram Bot (inline keyboards)
- Серверная карточка отправляется как фото с PNG-графиком онлайна
- Inline-кнопки: **📊 24ч / 7д / 30д** — меняют период прямо в сообщении
- Кнопка **🔗 Подключиться** (steam:// / иной deeplink)
- Поддержка тем (thread_id) для форумных каналов

### PNG-графики
- Эндпоинт `GET /api/v1/chart/:serverID?period=24h|7d|30d`
- Генерируется на Go (`go-chart/v2`): тёмный фон, neon-green линия, area-fill
- Используется в Discord- и Telegram-эмбедах
- Кэш 5 минут (`Cache-Control: public, max-age=300`)

### Панель администратора (`/admin`)
- **Пользователи** — поиск, фильтры, бан/разбан, смена роли, удаление; массовые операции
- **Серверы** — просмотр всех серверов с владельцами; массовое удаление
- **Статистика** — сводные карточки, проверка актуальности версии (GitHub Releases)
- **Аудит-лог** — история всех административных действий
- **Настройки сайта**:
  - Название и логотип
  - Steam API key (DB или ENV)
  - Тема по умолчанию (тёмная / светлая / системная)
  - Управление регистрацией (включить / выключить)
  - Force HTTPS
  - SSL (Let's Encrypt или кастомный сертификат)
  - Discord: App ID, Bot Token, webhook для новостей, ID канала алертов, интервал обновления
  - Telegram: Bot Token, Chat ID, Thread ID
  - Email (SMTP): задаётся через ENV
- **Бэкап / восстановление БД** — скачать SQL-дамп или загрузить резервную копию
- **Экспорт CSV** — список серверов (`/admin/export/servers.csv`) и игроков (`/admin/export/players.csv`)
- **Массовые операции** — плавающая панель с выбором действий для нескольких записей

### Безопасность
- Bcrypt хэширование паролей
- JWT с JTI — инвалидация при смене пароля / «выйти везде»
- Rate limiting на эндпоинтах авторизации
- X-API-Key middleware для server-to-server запросов
- 2FA / TOTP — второй фактор при входе

### Инфраструктура
- **Docker Compose** — MySQL 8.0, Go-бэкенд, Next.js frontend, Nginx reverse-proxy
- **Скрипт установки** (`install.sh`) для Ubuntu Server 24.04 (интерактивный, EN/RU, systemd)
- Автосоздание 2 ГБ swap — защита от OOM на VDS с 1 ГБ RAM
- **SSL/HTTPS** — Let's Encrypt (автообновление) или загрузка своего сертификата
- **502-страница** — кастомная с обратным отсчётом и EN/RU переключением
- RSS-лента новостей (`/feed.rss`)
- CI/CD workflow (GitHub Actions)

---

## Технологический стек

| Слой | Технология |
|------|------------|
| Бэкенд | Go 1.24, Echo v4, GORM v2 |
| База данных | MySQL 8.0 |
| Фронтенд | Next.js 14, TypeScript, Tailwind CSS |
| Состояние | React Query (TanStack Query) |
| Real-time | WebSocket (gorilla/websocket) |
| Графики (UI) | Recharts |
| Графики (PNG) | go-chart/v2 |
| Discord Bot | discordgo (Gateway) |
| 2FA | pquerna/otp (TOTP) |
| Иконки | lucide-react |
| Инфраструктура | Docker Compose, Nginx, systemd |

---

## Быстрый старт (Docker)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/RJ-Bond/js-monitoring
cd js-monitoring

# 2. Настроить конфигурацию
cp .env.example .env
nano .env   # задать JWT_SECRET, DB_PASSWORD и пр.

# 3. Запустить
docker compose up -d --build

# 4. Открыть в браузере → откроется /setup для создания администратора
# http://localhost
```

---

## Установка на Ubuntu Server 24.04

```bash
curl -fsSL https://raw.githubusercontent.com/RJ-Bond/js-monitoring/main/install.sh | sudo bash
```

Скрипт автоматически:
- Создаёт 2 ГБ swap (защита от OOM на 1 ГБ VDS)
- Устанавливает Docker + Docker Compose
- Клонирует репозиторий в `/opt/js-monitoring`
- Генерирует случайные безопасные пароли
- Настраивает UFW (SSH + HTTP/HTTPS)
- Регистрирует systemd-сервис для автозапуска
- Собирает и запускает все контейнеры

---

## Переменные окружения

Полный список — [`.env.example`](.env.example)

| Переменная | Описание |
|------------|----------|
| `DB_PASSWORD` | Пароль MySQL |
| `JWT_SECRET` | Секрет для подписи JWT (случайная строка 64+ символа) |
| `STEAM_API_KEY` | API-ключ Steam для входа через Steam ([получить](https://steamcommunity.com/dev/apikey)) |
| `APP_URL` | Публичный URL приложения (для Steam OpenID callback и Discord/Telegram изображений) |
| `SMTP_HOST` | SMTP-сервер для email-уведомлений |
| `SMTP_PORT` | Порт SMTP (обычно 587) |
| `SMTP_USER` | Логин SMTP |
| `SMTP_PASS` | Пароль SMTP |
| `SMTP_FROM` | Адрес отправителя |
| `HTTPS_PROXY` | HTTP-прокси для Discord Gateway (опционально) |

> Discord Bot Token, Telegram Bot Token, Steam API Key и другие секреты можно задать прямо в панели администратора — они сохраняются в БД и не требуют перезапуска (кроме Discord Bot Token).

---

## Структура проекта

```
js-monitoring/
├── backend/
│   ├── cmd/server/main.go          # Точка входа, роутинг Echo
│   └── internal/
│       ├── models/                 # GORM-модели (User, Server, NewsItem, SiteSettings…)
│       ├── database/               # Подключение к MySQL, AutoMigrate
│       ├── poller/                 # Опросчик: A2S, SAMP, Minecraft, FiveM
│       ├── api/                    # Echo-хэндлеры, WebSocket хаб, chart.go
│       ├── bot/                    # Discord Gateway bot, Telegram poller
│       └── notify/                 # Email-уведомления (SMTP)
├── frontend/
│   └── src/
│       ├── app/                    # Next.js App Router (страницы)
│       │   ├── page.tsx            # Главная с серверами и новостями
│       │   ├── admin/              # Панель администратора
│       │   ├── profile/            # Личный кабинет
│       │   ├── u/[username]/       # Публичный профиль пользователя
│       │   ├── player/[name]/      # Публичный профиль игрока
│       │   ├── compare/            # Сравнение серверов
│       │   ├── embed/[serverID]/   # Встраиваемый виджет
│       │   ├── login/ register/    # Авторизация
│       │   └── setup/              # Первоначальная настройка
│       ├── components/             # ServerCard, SiteBrand, GameIcon, Toast…
│       ├── contexts/               # AuthContext, LanguageContext, SiteSettingsContext, ThemeContext
│       ├── hooks/                  # useServers, useWebSocket, useFavorites…
│       ├── lib/                    # api.ts, utils.ts, translations.ts, version.ts
│       └── types/                  # TypeScript-интерфейсы
├── nginx/
│   ├── nginx.conf                  # Reverse proxy + WebSocket проксирование
│   └── 502.html                    # Кастомная страница обслуживания
├── mysql/init.sql                  # Инициализация БД
├── docker-compose.yml              # Все сервисы
├── install.sh                      # Установщик для Ubuntu 24.04 (RU/EN)
└── .env.example                    # Шаблон конфигурации
```

---

## Управление после установки

```bash
# Статус сервиса
systemctl status js-monitoring

# Логи в реальном времени
docker compose -f /opt/js-monitoring/docker-compose.yml logs -f

# Перезапуск
systemctl restart js-monitoring

# Обновление до последней версии
curl -fsSL https://raw.githubusercontent.com/RJ-Bond/js-monitoring/main/install.sh | sudo bash
```

---

## Лицензия

MIT — свободное использование, модификация и распространение.
